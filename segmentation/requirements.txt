torch>=2.1
timm>=1.0.16              # eva02-l, swinv2-l (feature extraction)
transformers>=4.44        # dinov2-l backbone (facebook/dinov2-large)
albumentations>=1.4
opencv-python>=4.8
numpy>=1.24
pandas>=2.0
PyYAML>=6.0
huggingface-hub>=0.23
wandb>=0.16               # optional: only needed when wandb.enabled=true

# InternImage-L (backbone 'internimage-l') loads via transformers
# trust_remote_code from https://huggingface.co/OpenGVLab/internimage_l_22k_384 .
# Its remote code includes a pure-PyTorch DCNv3 fallback, so NO custom CUDA op
# build is required (the CUDA op is faster if you compile it from
# https://github.com/OpenGVLab/InternImage , but it is optional).
